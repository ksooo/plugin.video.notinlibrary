<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="plugin.video.notinlibrary"
       name="Not in Library"
       version="1.0.0"
       provider-name="ksooo">
  <requires>
    <import addon="xbmc.python" version="3.0.0"/>
  </requires>
  <extension point="xbmc.python.pluginsource" library="main.py">
    <provides>video</provides>
  </extension>
  <extension point="xbmc.addon.metadata">
    <summary lang="en_GB">Find videos in your sources that are missing from the video library</summary>
    <description lang="en_GB">Not in Library walks through all video sources of the active Kodi profile and lists every video file that has no matching entry in the video library.[CR][CR]The original folder structure is preserved: the top level shows your video sources, below that only those directories that actually contain videos missing from the library, and the leaf level shows the files themselves. Files can be played straight from the list, and Kodi's own Information entry scrapes a single one of them into the library. Videos that should never be imported - trailers, home videos, sample files - can be excluded, individually or by folder.[CR][CR]This makes it easy to spot media that was never picked up by the scraper - because of a naming issue, a missing content setting, or simply because the library scan was never run for that folder.</description>
    <disclaimer lang="en_GB">This add-on only reads your sources and the video library. It never moves, renames or deletes any file.</disclaimer>
    <platform>all</platform>
    <license>GPL-2.0-or-later</license>
    <source>https://github.com/ksooo/plugin.video.notinlibrary</source>
    <news>v1.0.0 (2026-08-16)
- Initial release.</news>
    <assets>
      <icon>icon.png</icon>
    </assets>
  </extension>
</addon>
